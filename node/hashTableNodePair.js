export default class {
  constructor(key, value) {
    this._key = key
    this._value = value
  }

  set setKey(value) {
    this._key = value
  }

  set setValue(value) {
    this._value = value
  }
}